{% extends 'base.html.twig' %}

{% block body %}
{{ parent() }}
    <h1>Tag creation</h1>

    {{ form_start(form) }}
    <div class='input-filed'>
      {{form_row(form.name)}}
    </div>
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('administration_tag_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
